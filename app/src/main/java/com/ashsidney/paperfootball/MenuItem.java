package com.ashsidney.paperfootball;

import android.content.res.Resources;

import org.xmlpull.v1.XmlPullParser;


/*public class MenuItem extends TextSprite
{
  public MenuItem (Menu owner)
  {
    this.owner = owner;
  }

  public boolean press (float[] point)
  {
    return point[0] >= 0 && point[0] < getWidth() && point[1] >= 0 && point[1] < getHeight()
      && action.execute();
  }

  @Override
  protected boolean loadAttribute (XmlPullParser parser, Resources res)
  {
    if (!super.loadAttribute(parser, res))
    {
      switch (parser.getName())
      {
        case ActionTag:
          action = MenuActionFactory.generate(owner, parser);
          return true;
      }
      return false;
    }
    return true;
  }
  
  public static final String Tag = "menu_item";
  public static final String ActionTag = "action";

  /// vlastnik menu polozky
  protected Menu owner = null;
  /// akcia vykonana po zvoleni menu
   protected MenuAction action = null;
}*/
